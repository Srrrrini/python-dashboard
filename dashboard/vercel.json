{
    "builds": [{
        "src": "dashboard/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python4.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dashboard/wsgi.py"
        }
    ]
}
